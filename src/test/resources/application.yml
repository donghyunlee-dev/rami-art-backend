spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0&preferQueryMode=simple}
    username: ${DB_USERNAME:postgres.gipaatdvbnmahvpwphtk}
    password: ${DB_PASSWORD:!@!L1e2e34!!!}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    default-schema: rami_art_studio
    schemas: rami_art_studio
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 1

app:
  version: 0.0.1-SNAPSHOT
  security:
    jwt-secret: rami-art-studio-secret-key-change-this-in-production-2026
    access-token-expiration-seconds: 3600
    refresh-token-expiration-seconds: 1209600
    bootstrap-admin-email: admin@ramiartstudio.com
    bootstrap-admin-name: 원장
    bootstrap-admin-password: ChangeMe123!
